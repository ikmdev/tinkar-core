package org.hl7.tinkar.test;

/**
 * Here to prevent module-info error for empty package.
 * Sources are extracted/generated then compiled.
 * So IntelliJ thinks the package is empty, when it is not.
 */
public interface NoOp {
}
