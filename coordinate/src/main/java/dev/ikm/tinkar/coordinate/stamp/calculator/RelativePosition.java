package dev.ikm.tinkar.coordinate.stamp.calculator;

public enum RelativePosition {
    /** The before. */
    BEFORE,

    /** The equal. */
    EQUAL,

    /** The after. */
    AFTER,

    /** The contradiction. */
    CONTRADICTION,

    /** The unreachable. */
    UNREACHABLE
}
